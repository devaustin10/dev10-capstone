// const [selectedState, setSelectedState] = useState();
// const [filteredTrails, setFilteredTrails] = useState();


// // Need 2 Forms
//     // First Form fetches all trails and then...
//         setSelectedState(dropdownPick)
//         // Submit button will call the filter method
//         const filterTrailsByState = (dropDownStateSelection) => {
//             return trails.filter(trail => {
//                 return trail.state = dropDownStateSelection;
//             })
//         }

//        setFilteredTrails(filterTrailsByState());

//     // Build Second Form using filteredTrails
//         // build out trails dropdown from this data


// let revisedHike = {
//     state: selectedState,
//     ...secondFormsData
// }

function AddHikeForm({ trails }) {
    
}

export default AddHikeForm;